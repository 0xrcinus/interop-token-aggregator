# PostgreSQL Configuration (Neon-compatible variable names)
# For local Docker development:
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DATABASE=tokendb
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_SSL=false

# Alternatively, use DATABASE_URL for connection string:
# DATABASE_URL=postgresql://dev:dev@localhost:5433/tokendb

# For Neon production, use the provided environment variables:
# DATABASE_URL=<from Neon dashboard>
# POSTGRES_HOST=<from Neon dashboard>
# POSTGRES_PORT=5432
# POSTGRES_USER=<from Neon dashboard>
# POSTGRES_DATABASE=<from Neon dashboard>
# POSTGRES_PASSWORD=<from Neon dashboard>
# POSTGRES_SSL=true

# Admin API secret for triggering fetches
ADMIN_SECRET=change-this-in-production

# Page revalidation interval in seconds (default: 300 = 5 minutes)
# Lower values = fresher data but more database load
# Recommended: 300 (5 min) for dev, 600 (10 min) for production, 3600 (1 hour) for low-traffic
NEXT_PUBLIC_REVALIDATE_INTERVAL=300

# Note: To add custom domains to the devcontainer firewall (like Neon database):
# 1. Copy .devcontainer/custom-domains.txt.example to .devcontainer/custom-domains.txt
# 2. Add your domains (one per line)
# 3. Rebuild the devcontainer
# See DEVCONTAINER_SETUP.md for details
